[tox]
requires =
    tox>=4

[testenv]
description = run unit tests
deps =
    pytest>=8
    pytest-asyncio
    pytest-cov
    -r issuer-lib/requirements.txt
    -r owner-lib/requirements.txt
    -r service-lib/requirements.txt
commands =
    pytest --cov=issuer-lib/issuer --cov-report=xml issuer-lib
    pytest --cov=owner-lib/owner --cov-append --cov-report=xml owner-lib
    pytest --cov=service-lib/service --cov-append --cov-report=xml service-lib
    coverage html
setenv =
    file|.env
